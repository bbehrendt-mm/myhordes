type GroupDefs = {
    order: number,
    title: string,
}

export type TranslationStrings = {
    add: string,
    remove: string,
    agree: string,
    close: string,
    emote_groups: {[key: string]: GroupDefs}
}
