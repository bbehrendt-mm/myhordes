export type TranslationStrings = {
    common: {
        defense: string,
        defense_base: string,
        defense_bonus: string,
        defense_temp: string,
        state: string,
        level: string,

        show_list: string,
        close: string,
    }
}