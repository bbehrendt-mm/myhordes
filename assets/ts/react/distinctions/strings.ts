export type TranslationStrings = {
    common: {
        header: string
        points: string
        tab_picto: string
        tab_award: string
    },
    pictos: {
        empty: string,
    },
    awards: {
        unique: string,
        unique_desc: string,
        unique_url: string,
        single: string,
        single_desc: string,
    }
}