{% block ajax %}
    {% for user in users %}
        <div class="user-list-entry">
        <button x-ajax-href="{{ url('admin_users_citizen_view', {id: user.id}) }}" class="button">
            {{ user.username }}
        </button>
        </div>
    {% endfor %}
{% endblock %}