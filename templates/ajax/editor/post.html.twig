{% extends "ajax/ajax_plain.html.twig" %}
{% block ajax_base %}
    {% from 'ajax/editor/base/forum-editor.html.twig' import forumEditor as editor %}
    {{ editor(
        app.user,
        username,
        permission,
        alias,
        pid is not null ? 'Bearbeiten'|trans({},'global') : 'Neuer Post'|trans({},'global'),
        town_controls,
        edit_title,
        edit_title,
        pid is null,
        pid is not null ? path('forum_edit_post_controller', {fid: fid, tid: tid, pid: pid}) : path('forum_new_post_controller', {fid: fid, tid: tid}),
        tags,
        current_tag,
    ) }}
{% endblock %}