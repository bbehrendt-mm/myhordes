{% extends "ajax/ajax_plain.html.twig" %}
{% block ajax_base %}
    {% from 'ajax/editor/base/announcement-editor.html.twig' import announcementEditor as editor %}
    {{ editor(
        null,
        app.user,
        permission,
        'changelog',
        'Neue Update'|trans({},'admin'),
        true,
        path('admin_changelog_new_changelog')
    ) }}
{% endblock %}