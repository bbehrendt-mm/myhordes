{% extends "ajax/game/game.html.twig" %}
{% block title %}{{'Auszeichnungsdetails'|trans({}, 'game')}}{% endblock %}
{% block ajax %}
    <div class="row soul">
        <div class="cell padded header rw-10 ro-1 rw-md-12 ro-md-0 edge-top-left">
            <div class="tabs">
                <div class="tab-floater">
                    <div class="tab left inline">
                        {% include "ajax/soul/playeravatar.html.twig" with {user: user, small: true, fallback: false} only %}
                        <h4 class="soul-name">
                            <img src="{{ asset('build/images/icons/small_ghost_red.gif') }}" alt="{{'Held'|trans({}, 'game')}}" />
                            {% include 'ajax/soul/playername.html.twig' with {user: user, tag: 'a'} only %}
                        </h4>
                    </div>
                    <div class="tab tab-floater">
                        <button x-ajax-href='{{path('soul_visit', {id: user.id})}}' style="margin-top: 4px;padding: 0 8px;" class="inline"><span class="hide-sm">{{ "Kartei schließen"|trans({}, 'soul') }}</span><span class="hide-lg hide-md hide-desktop"><img style="margin-right: 0;" src="{{ asset('build/images/icons/small_remove.gif') }}" alt="[X]" /></span></button>
                    </div>
                </div>
            </div>

            <div class="row-flex bottom hide-mobile" style="position: relative">
                <div class="padded cell rw-4 rw-sm-12 center">
                    <h5>{{ 'Auf MyHordes erworbene Auszeichnungen'|trans({},'soul') }}</h5>
                </div>

                <div class="padded cell rw-4 rw-sm-12 center">
                    <h5>{{ 'Importierte Auszeichnungen'|trans({},'soul') }}</h5>
                </div>

                <div class="padded cell rw-4 rw-sm-12 center">
                    <h5>{{ 'Alte Auszeichnungen'|trans({},'soul') }}</h5>
                </div>
            </div>

            <div class="row">
                <div class="padded cell rw-4 rw-sm-12 center">
                    <h5 class="hide-desktop">{{ 'Auf MyHordes erworbene Auszeichnungen'|trans({},'soul') }}</h5>
                    <hordes-distinctions data-plain="1" data-source="mh" data-user="{{ user.id }}"></hordes-distinctions>
                </div>

                <div class="padded cell rw-4 rw-sm-12 center">
                    <h5 class="hide-desktop">{{ 'Importierte Auszeichnungen'|trans({},'soul') }}</h5>
                    <hordes-distinctions data-plain="1" data-source="imported" data-user="{{ user.id }}"></hordes-distinctions>
                </div>

                <div class="padded cell rw-4 rw-sm-12 center">
                    <h5 class="hide-desktop">{{ 'Alte Auszeichnungen'|trans({},'soul') }}</h5>
                    <hordes-distinctions data-plain="1" data-source="old" data-user="{{ user.id }}"></hordes-distinctions>
                </div>
            </div>

        </div>

    </div>
{% endblock %}
{% block js %}
    {{parent()}}
{% endblock %}
