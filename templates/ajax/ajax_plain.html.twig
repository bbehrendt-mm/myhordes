<!DOCTYPE html>
<html lang="{{ app.request.locale }}" x-ajax-intention="inline">
    <body>{% block ajax_base %}{% endblock %}</body>
    {% block js %}
        {% if is_granted('ROLE_USER') %}
            <script>
                $.msg.jumpstart();
            </script>
        {% endif %}
    {% endblock %}
</html>