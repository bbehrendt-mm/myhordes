{% apply spaceless %}
    <hordes-service-worker-indicator
            data-text-title="{{ 'Dieser Indikator zeigt an, ob du aktuell mit dem Live-Dienst von MyHordes verbunden bist.'|trans({},'global') }}"
            data-text-help="{{ 'Mithilfe des Live-Dienstes erhälst dein System Benachrichtigungen vom MyHordes-Server in Echtzeit, beispielsweise über neue Forenposts oder PNs.'|trans({},'global') }}"
            data-text-no-sw="{{ 'Aktuell besteht keine Verbindung zum Live-Dienst. Du kannst dennoch wie gewohnt weiter spielen, da alle wichtigen Funktionen auch ohne den Live-Dienst arbeiten können.'|trans({},'global') }}"
            data-text-offline="{{ 'Nicht verbunden'|trans({},'global') }}"
            data-text-connecting="{{ 'Verbindung wird hergestellt'|trans({},'global') }}"
            data-text-upgrading="{{ 'Verbunden, Verbindungsupdate angefordert'|trans({},'global') }}"
            data-text-online="{{ 'Verbunden'|trans({},'global') }}"
    ></hordes-service-worker-indicator>
{% endapply %}